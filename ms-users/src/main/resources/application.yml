# Server properties
server:
  servlet:
    context-path: /ms-users-api
# Spring properties
spring:
  # Dev is active by default. This value can be overwritten by java arg param 
  profiles:
    active: dev
  application:
    name: ms-users
  messages:
    basename: bundles/apiLanguageBundle
  jackson:
    serialization:
      write-dates-as-timestamps: false
springdoc:
  swagger-ui:
    enabled: true

